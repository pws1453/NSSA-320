all:
  children:
    winnet:
      hosts:
        domain_controller:
          ansible_host: 10.0.2.15
          uname: Administrator
          password: P0gword
          hname: central
          fqdn: central.winnet.local
      vars:
        domain_name: winnet.local
