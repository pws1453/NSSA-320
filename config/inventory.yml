all:
  children:
    winnet:
      hosts:
        domain_controller:
          ansible_host: 10.0.3.104
          hname: central
          fqdn: central.winnet.local
      vars:
        domain_name: winnet.local
